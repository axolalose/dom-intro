describe("calculateBill function",function(){
    it("Should return all the products that have a quantity higher than 20",function(){
        var testBill = calculateBill();
        testBill.add('sms')
        assert.equal(testBill.total(),);
        })
    })